<% content_for(:title, t('.title')) %>

<div class="container py-4">
  <script src="https://accounts.google.com/gsi/client" async defer></script>

    <div class="row">
      <div class="col-12 col-lg-11 mx-auto">

        <!-- タイトル -->
        <h2><%= t('.title') %></h2>

            <!-- 通常ログインフォーム -->
            <div class="bg-white bg-opacity-50 rounded-3 py-4 my-3">
              <div class="row">
                <div class="col-11 col-lg-10 mx-auto text-center">

                  <%= form_with url: login_path, method: :post, local: true do |f| %>
                  <!-- メールアドレス入力欄 -->
                  <div class="mb-3">
                    <%= f.label :email %>
                    <%= f.email_field :email, class: "form-control" %>
                  </div>
                  <!-- パスワード入力欄 -->
                  <div class="mb-3">
                    <%= f.label :password %>
                    <%= f.password_field :password, class: "form-control" %>
                  </div>

                  <!-- ログイン状態の保持 -->
                  <div>
                    <%= check_box_tag :remember, params[:remember], true %>
                    <%= label_tag :remember, 'ログイン状態を保持する' %>
                  </div>

                  <!-- ログインボタン -->
                  <%= f.submit "ログイン", class: "btn btn-secondary" %>
                  <% end %>

                  <!-- パスワード再設定ボタン -->
                  <div class="text-center py-1">
                    <%= link_to 'パスワードをお忘れの方', new_password_reset_path %>
                  </div>
                </div>
              </div>
            </div>

            <!-- googleログインフォーム -->
            <div class="bg-white bg-opacity-50 rounded-3 py-4 my-3">
              <div class="row">
              <div class="col-11 col-lg-10 mx-auto text-center">

                <div class="d-inline-block bg-primary text-white fw-bold rounded-2 px-3 py-2 m-2">Googleアカウントで会員登録した方はこちら</div>
                  <div class="m-3">
                    <div id="g_id_onload"
                      data-client_id="934784863584-07bc30bi7qd21in005c81pf4023f3lgq.apps.googleusercontent.com"
                      data-login_uri="https://www.healscan.jp/auth/google_oauth2/callback"
                      data-state="login"
                      data-auto_prompt="false">
                    </div>
                    <div class="g_id_signin"
                      data-type="standard"
                      data-size="large"
                      data-theme="outline"
                      data-text="sign_in_with"
                      data-shape="rectangular"
                      data-logo_alignment="left">
                    </div>
                  </div>
                </div>
              </div>
            </div>

        <!-- 新規登録ボタン -->
        <div class='text-end'>
          <%= link_to t('user_sessions.new.to_register_page'), new_user_path, class: 'btn btn-secondary' %>
        </div>
    </div>
  </div>
</div>
